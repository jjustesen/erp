import styled from "styled-components";
import {
  layout,
  border,
  shadow,
  position,
  space,
  color,
  background,
  grid,
} from "styled-system";

export const Box = styled.div`
  ${layout};
  ${border};
  ${shadow};
  ${position};
  ${space};
  ${color};
  ${background};
  ${grid};
`;
